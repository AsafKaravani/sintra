# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on-push to main
'on':
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    environment: sintra-server-dev
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./services/client

    steps:
      - uses: actions/checkout@v2
      - run: echo "$VITE_FB_API_KEY"
      - run: yarn install && yarn build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          entryPoint: ./services/client
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_SINTRA_APP_DEV }}'
          channelId: live
          projectId: sintra-app-dev
